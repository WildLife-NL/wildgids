# wildgids
This is the new repository for Wild Gids app.

The Flutter application lives in the [`src/`](./src/) directory. From the repository root, run `cd src` before `flutter pub get` or `flutter run`.
